language: python

sudo: false

python:
  - 2.7
  - 3.5
  - 3.6

install:
  - pip install Hydrostats
  - pip install codecov

script:
  - cd hydrostats/tests
  - coverage run tests.py

after_success:
  - codecov
